# Proxy API calls to Render so login cookies stay on the same domain as the site.
# (Cross-site cookies from pages.dev -> onrender.com are blocked by most browsers.)
/api/*  https://bot-wf8x.onrender.com/api/:splat  200
/*    /index.html   200
